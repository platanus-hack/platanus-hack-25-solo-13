---
const steps = [
  {
    number: '01',
    title: 'crea tu cuenta',
    description: 'cuéntanos qué te gusta y cómo aprendes mejor · toma 2 minutos',
    color: 'from-emerald-500 to-teal-600'
  },
  {
    number: '02',
    title: 'test rápido',
    description: 'unas preguntas cortitas para saber tu nivel · sin estrés',
    color: 'from-teal-500 to-cyan-600'
  },
  {
    number: '03',
    title: 'tu plan se arma solo',
    description: 'recibe tu ruta personalizada con todo lo que necesitas',
    color: 'from-cyan-500 to-sky-600'
  },
  {
    number: '04',
    title: 'a darle nomás',
    description: 'aprende, sube de nivel, desbloquea logros · 100% a tu ritmo',
    color: 'from-emerald-500 to-yellow-500'
  }
];
---

<section id="como-funciona" class="py-20 px-4 sm:px-6 lg:px-8 bg-gradient-to-br from-emerald-50 via-cyan-50 to-teal-50">
  <div class="max-w-7xl mx-auto">
    <div class="text-center mb-16">
      <div class="inline-block mb-3 px-4 py-1 bg-cyan-100 text-cyan-700 rounded-full text-sm font-semibold">
        🚀 súper fácil de empezar
      </div>
      <h2 class="text-4xl sm:text-5xl font-extrabold text-gray-900 mb-4 lowercase">
        en <span class="text-transparent bg-clip-text bg-gradient-to-r from-emerald-600 to-cyan-600">4 pasos</span> estás dentro
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        literal te demoras menos que en hacer una cuenta de Instagram
      </p>
    </div>

    <div class="relative">
      <!-- Connection line -->
      <div class="hidden lg:block absolute top-1/2 left-0 right-0 h-1 bg-gradient-to-r from-emerald-300 via-cyan-300 to-yellow-300 transform -translate-y-1/2 opacity-30"></div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        {steps.map((step, index) => (
          <div class="relative">
            <div class="bg-white rounded-2xl p-6 shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border-2 border-transparent hover:border-emerald-200">
              <div class={`inline-block text-6xl font-extrabold text-transparent bg-clip-text bg-gradient-to-br ${step.color} mb-4`}>
                {step.number}
              </div>
              <h3 class="text-xl font-bold text-gray-900 mb-3">
                {step.title}
              </h3>
              <p class="text-gray-600 leading-relaxed">
                {step.description}
              </p>

              {/* Arrow for desktop */}
              {index < steps.length - 1 && (
                <div class="hidden lg:block absolute top-1/2 -right-4 transform -translate-y-1/2 z-10">
                  <svg class="w-8 h-8 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M13 7l5 5m0 0l-5 5m5-5H6" />
                  </svg>
                </div>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>

    <div class="mt-16 text-center">
      <div class="inline-block bg-white rounded-2xl p-8 shadow-xl border-2 border-emerald-100">
        <p class="text-lg text-gray-700 mb-5">
          <span class="font-bold text-emerald-600 text-xl">ya somos +10.000</span> <br/>
          <span class="text-base">estudiantes aprendiendo a su modo 🔥</span>
        </p>
        <div class="flex flex-wrap items-center justify-center gap-4 text-sm text-gray-700">
          <div class="flex items-center">
            <span class="text-lg mr-1.5">✓</span>
            <span class="lowercase font-medium">gratis siempre</span>
          </div>
          <div class="flex items-center">
            <span class="text-lg mr-1.5">✓</span>
            <span class="lowercase font-medium">sin tarjeta</span>
          </div>
          <div class="flex items-center">
            <span class="text-lg mr-1.5">✓</span>
            <span class="lowercase font-medium">puedes salir cuando quieras</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
