---
const features = [
  {
    icon: '🎯',
    title: '100% personalizado',
    bullets: ['aprende a tu ritmo', 'temas que te interesan', 'como TÚ prefieres']
  },
  {
    icon: '📚',
    title: 'toda la malla chilena',
    bullets: ['todas las materias', 'enseñanza media completa', 'super organizado']
  },
  {
    icon: '🧠',
    title: 'se adapta a ti',
    bullets: ['cambia según avanzas', 'más fácil o difícil', 'siempre el nivel justo']
  },
  {
    icon: '🎮',
    title: 'cero fome',
    bullets: ['misiones y desafíos', 'logros desbloqueables', 'recompensas épicas']
  },
  {
    icon: '📊',
    title: 'trackea todo',
    bullets: ['ve tu progreso real', 'dashboards interactivos', 'sabe dónde mejorar']
  },
  {
    icon: '👥',
    title: 'aprende con otros',
    bullets: ['conecta con estudiantes', 'comparte lo que sabes', 'comunidad activa']
  }
];
---

<section id="caracteristicas" class="py-20 px-4 sm:px-6 lg:px-8 bg-white">
  <div class="max-w-7xl mx-auto">
    <div class="text-center mb-16">
      <div class="inline-block mb-3 px-4 py-1 bg-emerald-100 text-emerald-700 rounded-full text-sm font-semibold">
        ⚡ lo que hace a Lumera diferente
      </div>
      <h2 class="text-4xl sm:text-5xl font-extrabold text-gray-900 mb-4">
        por qué te va a <span class="text-transparent bg-clip-text bg-gradient-to-r from-emerald-600 to-cyan-600">encantar</span>
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        diseñado por y para estudiantes como tú
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {features.map((feature) => (
        <div class="group relative bg-gradient-to-br from-white to-emerald-50 rounded-2xl p-7 shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border-2 border-emerald-100 hover:border-emerald-300">
          <div class="text-6xl mb-3 transform group-hover:scale-110 group-hover:rotate-6 transition-all duration-300">
            {feature.icon}
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-4 lowercase">
            {feature.title}
          </h3>
          <ul class="space-y-2">
            {feature.bullets.map((bullet) => (
              <li class="flex items-start text-gray-700">
                <span class="text-emerald-500 mr-2 mt-0.5">✓</span>
                <span class="text-sm lowercase">{bullet}</span>
              </li>
            ))}
          </ul>
          <div class="mt-5 h-1 w-0 group-hover:w-full bg-gradient-to-r from-emerald-500 to-cyan-500 transition-all duration-500 rounded-full"></div>
        </div>
      ))}
    </div>
  </div>
</section>
