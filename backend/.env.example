# Backend Environment Variables

# Server
PORT=8080

# Database (use 'postgres' as host in Docker, 'localhost' when running locally)
DB_HOST=postgres
DB_USER=admin
DB_PASSWORD=hackathon2025
DB_NAME=hackathon
DB_PORT=5432
